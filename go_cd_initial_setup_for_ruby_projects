The below is the definition of task:

Command*
bash


Arguments
-c
\curl -sSL https://rvm.io/mpapis.asc | gpg2 --import -
\curl -sSL https://get.rvm.io | bash -s stable --ruby

rvm install 2.0.0

rvm list
rvm use 2.0.0
gem install bundler
bundle install
